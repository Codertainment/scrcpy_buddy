# Supported scrcpy options

This directory contains documents for the supported scrcpy options by scrcpy buddy.

- Each option must be inside a category
- Each category has it's own `.md` file
- Options can be either marked as "Advanced" or not
