import 'package:fluent_ui/fluent_ui.dart';

class ConfigDivider extends StatelessWidget {
  const ConfigDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.only(bottom: 8), child: Divider());
  }
}
